apiVersion: v1
kind: Service
metadata:
  name: {{ include "assignmentor.fullname" . }}
  labels:
    {{- include "assignmentor.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "assignmentor.selectorLabels" . | nindent 4 }}
